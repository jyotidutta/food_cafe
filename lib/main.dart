import 'package:flutter/material.dart';
import 'package:food_cafe/Home_page/Home_pages.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
